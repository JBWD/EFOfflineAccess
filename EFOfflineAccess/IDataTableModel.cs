using System;
using System.Collections.Generic;
using System.Text;

namespace EFOfflineModels
{
    /// <summary>
    /// Defines the contract for a data table model, providing a common interface for representing tabular data
    /// structures.
    /// </summary>
    /// <remarks>Implementations of this interface typically encapsulate the logic for accessing,
    /// manipulating, and presenting data in a tabular format. This interface is intended to be used as a base for data
    /// table models in applications that require abstraction over tabular data sources.</remarks>
    public interface IDataTableModel
    {
    }
}
